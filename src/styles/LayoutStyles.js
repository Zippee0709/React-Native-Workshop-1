import { StyleSheet } from 'react-native';

const LayoutScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#ffffff',
    },
});

export default LayoutScreenStyles;
